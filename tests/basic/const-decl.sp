#include <shell>

const float sBlah = 1.0, int sCrab = 10;

public main() {
  printfloat(sBlah);
  printnum(sCrab);

  const float kBlah = 2.0, int kCrab = 12;
  printfloat(kBlah);
  printnum(kCrab);
}
