# vim: set sts=2 ts=8 sw=2 tw=99 et ft=python:
import os

binary = SP.SpProgram(builder, 'smxdump')

binary.compiler.cxxincludes += [
    os.path.join(builder.sourcePath),
]
binary.sources += [
    'smxdump.cpp',
]

builder.Add(binary)
